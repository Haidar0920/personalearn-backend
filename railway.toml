[build]
builder = "NIXPACKS"

[deploy]
startCommand = "./gradlew bootRun"
healthcheckPath = "/api-persona-learn/actuator/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "personalearn-backend"
